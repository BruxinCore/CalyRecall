<Page x:Class="CalyRecallNative.Views.Pages.RestorePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Backups">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        
        <Storyboard x:Key="EntranceAnimation">
            <DoubleAnimation Storyboard.TargetName="ContentGrid" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ContentTransform" Storyboard.TargetProperty="Y" From="15" To="0" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Page.Resources>

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard Storyboard="{StaticResource EntranceAnimation}" />
        </EventTrigger>
    </Page.Triggers>

    <Grid>
        <Grid x:Name="ContentGrid" Margin="42,32,42,42">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="ContentTransform" Y="15" />
        </Grid.RenderTransform>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions><StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{DynamicResource Restore_Title}" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
            <TextBlock Text="{DynamicResource Restore_Desc}" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel><Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <ui:Button Grid.Column="0" Content="{DynamicResource Restore_RefreshBtn}" Command="{Binding ViewModel.LoadBackupsCommand}" Icon="{ui:SymbolIcon ArrowClockwise24}" Appearance="Transparent" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            
            <ui:TextBox Grid.Column="1" Margin="16,0" PlaceholderText="Procurar backup..." Text="{Binding ViewModel.SearchText, UpdateSourceTrigger=PropertyChanged}" Icon="{ui:SymbolIcon Search24}" ClearButtonEnabled="True" HorizontalAlignment="Stretch" MaxWidth="600" />
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <ui:Button Content="Cancelar" Command="{Binding ViewModel.ClearSelectionCommand}" Appearance="Secondary" Margin="0,0,8,0" Visibility="{Binding ViewModel.HasSelectedItems, Converter={StaticResource BoolToVis}}" />
                <ui:Button Content="{Binding ViewModel.DeleteSelectedText}" Command="{Binding ViewModel.DeleteSelectedCommand}" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" Margin="0,0,8,0" Visibility="{Binding ViewModel.HasSelectedItems, Converter={StaticResource BoolToVis}}" />
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ViewModel.HasSelectedItems}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <ui:Button Content="{DynamicResource Restore_ImportBtn}" Command="{Binding ViewModel.ImportBackupCommand}" Icon="{ui:SymbolIcon ArrowDownload24}" Margin="0,0,8,0" />
                    <ui:Button Content="{DynamicResource Restore_ExportAllBtn}" Command="{Binding ViewModel.ExportAllBackupsCommand}" Icon="{ui:SymbolIcon Archive24}" Appearance="Primary" Background="#b28df9" BorderBrush="#b28df9" Foreground="Black" />
                </StackPanel>
            </StackPanel>
        </Grid><ui:Card Grid.Row="2" Padding="32,48" BorderThickness="1" BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Visibility="{Binding ViewModel.HasNoBackups, Converter={StaticResource BoolToVis}}" VerticalAlignment="Top" Margin="0,24,0,0">
            <StackPanel HorizontalAlignment="Center">
                <ui:SymbolIcon Symbol="Archive24" FontSize="32" Foreground="#b28df9" HorizontalAlignment="Center" Margin="0,0,0,16" />
                <TextBlock Text="{DynamicResource Restore_NoBackups}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,8" />
                <TextBlock Text="{DynamicResource Restore_NoBackupsDesc}" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" Width="300" TextWrapping="Wrap" Margin="0,0,0,24" />
            </StackPanel>
        </ui:Card><ListView Grid.Row="2" ItemsSource="{Binding ViewModel.Backups}" BorderThickness="0" Background="Transparent" Visibility="{Binding ViewModel.HasBackups, Converter={StaticResource BoolToVis}}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="CardBorder" Margin="0,0,16,16" Width="220" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="104" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions><Border Grid.Row="0" CornerRadius="11,11,0,0" ClipToBounds="True">
                                <Grid>
                                    <Image Source="{Binding HeaderImageUrl}" Stretch="UniformToFill" /><Grid HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Opacity" Value="0" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding ElementName=CardBorder, Path=IsMouseOver}" Value="False" />
                                                            <Condition Binding="{Binding DataContext.ViewModel.HasSelectedItems, RelativeSource={RelativeSource AncestorType=Page}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <MultiDataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </MultiDataTrigger.EnterActions>
                                                        <MultiDataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </MultiDataTrigger.ExitActions>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>

                                        <ToggleButton IsChecked="{Binding IsSelected, Mode=TwoWay}" Width="22" Height="22" Cursor="Hand" Padding="0" BorderThickness="0" Background="Transparent" FocusVisualStyle="{x:Null}">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Grid><Border Background="#40000000" CornerRadius="5" Margin="1">
                                                            <Border.Effect>
                                                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.6" Direction="270" />
                                                            </Border.Effect>
                                                        </Border><Border x:Name="BgBorder" Background="#80000000" BorderBrush="#A0FFFFFF" BorderThickness="1.5" CornerRadius="5" Margin="1" /><ui:SymbolIcon x:Name="CheckIcon" Symbol="Checkmark16" Foreground="Black" FontSize="14" Opacity="0" RenderTransformOrigin="0.5,0.5">
                                                            <ui:SymbolIcon.RenderTransform>
                                                                <ScaleTransform ScaleX="0.5" ScaleY="0.5" />
                                                            </ui:SymbolIcon.RenderTransform>
                                                        </ui:SymbolIcon>
                                                    </Grid>
                                                    <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="BgBorder" Property="Background" Value="#B3000000" />
                                                        </Trigger><Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="BgBorder" Property="Background" Value="#b28df9" />
                                                            <Setter TargetName="BgBorder" Property="BorderBrush" Value="#b28df9" />
                                                        </Trigger><EventTrigger RoutedEvent="ToggleButton.Checked">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetName="CheckIcon" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15" />
                                                                    <DoubleAnimation Storyboard.TargetName="CheckIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.25">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <BackEase Amplitude="0.5" EasingMode="EaseOut" />
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                    <DoubleAnimation Storyboard.TargetName="CheckIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.25">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <BackEase Amplitude="0.5" EasingMode="EaseOut" />
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                        <EventTrigger RoutedEvent="ToggleButton.Unchecked">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetName="CheckIcon" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.1" />
                                                                    <DoubleAnimation Storyboard.TargetName="CheckIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.5" Duration="0:0:0.1" />
                                                                    <DoubleAnimation Storyboard.TargetName="CheckIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.5" Duration="0:0:0.1" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                        </ToggleButton>
                                    </Grid>
                                </Grid>
                            </Border><StackPanel Grid.Row="1" Margin="16,12">
                                <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,0,0,4" />
                                
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <ui:SymbolIcon Symbol="Clock24" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding FormattedDate}" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                </StackPanel><Grid>
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.ViewModel.HasSelectedItems, RelativeSource={RelativeSource AncestorType=Page}}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ui:Button Content="{DynamicResource Restore_RestoreBtn}" Command="{Binding DataContext.ViewModel.RestoreBackupCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" Appearance="Secondary" HorizontalAlignment="Stretch" Margin="0,0,8,0" />
                                    
                                    <ui:Button Grid.Column="1" Command="{Binding DataContext.ViewModel.OpenEditModalCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" Appearance="Secondary" Icon="{ui:SymbolIcon Edit24}" ToolTip="{DynamicResource Restore_EditTooltip}" Margin="0,0,8,0" />
                                    
                                    <ui:Button Grid.Column="2" Command="{Binding DataContext.ViewModel.DeleteBackupCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" Appearance="Danger" Icon="{ui:SymbolIcon Delete24}" ToolTip="{DynamicResource Restore_DeleteTooltip}" />
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView><Grid Grid.Row="2" Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVis}}" Panel.ZIndex="10">
            <ui:ProgressRing IsIndeterminate="True" Width="64" Height="64" Foreground="#b28df9" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-48,0,0" />
        </Grid>
    </Grid>
</Grid>
</Page>
